#include <stdio.h>
#include <stdlib.h>





bool checarvalidade (int x, int y) {
    if (x < 9 && x > 0 && y < 9 && y > 0)
        return true;
    else 
        return false;
}

int main () {

    //Funcao para printar a matriz do usuario

    system ("pause")
    getchar (coordenada, coordenada) //espera o input do usuario

    if (checarvalidade(coordenada, coordenada) == false)
    
    






}