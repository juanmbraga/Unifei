#include <stdio.h>
/*Elabore um programa que leia uma lista de números inteiros até que um número negativo seja digitado. Para cada número lido, verificar se o número é divisor de 10 ou não. */

int main(void) {
  int num=0;

  while (num >= 0) {
    printf("Insira um numero inteiro: ");
    scanf("%d", &num);

    //verificar se e inteiro
    //verificar se e divisor
  }
  return 0;
}